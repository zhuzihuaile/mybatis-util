package com.zhuzi.mybatis.config;

public class Config {
	/** 是否开启 limit offset 验证  */
	public final static boolean IS_SORT = true;
	/** 是否开启 当in的个数为一时 使用 等于  */
	public final static boolean IS_IN = true;
}
